324CA Dumistracel Eduard-Costin
    In rezolvarea acestor probleme am realizat clasa matrix care salveaza
nr de noduri si numarul de legaturi si dimensiunea in cazul unei probleme.

Retele:
    Reducerea problemei la SAT:
        ->un nod nu se poate afla cu alt nod in aceeasi cicla daca nu are
            muchie intre ele
        ->un nod nu poate face parte din clica decat o data
        ->clica de k noduri si toate nodurile pot apartine clicai
        ->un nod face parte sigur dintre nodurile grafului
    Descifrarea mesajului:
        ->am verificat daca mesajul oracolului este "TRUE" si cautam
        fiecare elem > 0 pentru a determina nodurile din clica

Reclame:
    -> in cadrul acestei probleme realizam un imput corespunzator problemei
        de retele, dar de data asta dimensiunea incepe de la numarul de
        noduri si scade cu fiecare iteratie pana gasim conditia "True".
    -> dupa ce am gasit conditia "True", nodurile care ies in urma
        interpretarii acestora le vom stoca intr-un vector

Registe:
    -> poate sa fie redusa la problema colorarii unui graf in k culori
    Reducera problemei la SAT:
    -> fiecare nod poate sa fie colorat in k culori
    -> 2 noduri care au omuchie intre ele nu pot avea aceeasi culoare
    -> un nod are cel putin o culoare pentru primele N clauze si pentru
    urmatoarele clauze avem doar o culoare
